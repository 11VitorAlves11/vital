// One self-hosted variable family for the entire product. The package ships
// locally with the app, so no health-data surface depends on an external CDN.
import "@fontsource-variable/geist/wght.css";
