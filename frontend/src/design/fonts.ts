// Self-hosted fonts (SIL OFL) — no external CDNs.
//
// Atkinson Hyperlegible Next is the body face for the reason it exists: its
// letterforms are drawn to be told apart at a glance, so a 0 is not an O and a
// 1 is not an l. In a page made of measurements, that is not a stylistic point.
// The "Next" cut rather than the original because the UI needs 500 and 600,
// which the original does not ship.
import "@fontsource/public-sans/400.css";
import "@fontsource/public-sans/500.css";
import "@fontsource/public-sans/600.css";
import "@fontsource/atkinson-hyperlegible-next/400.css";
import "@fontsource/atkinson-hyperlegible-next/500.css";
import "@fontsource/atkinson-hyperlegible-next/600.css";
import "@fontsource/jetbrains-mono/400.css";
import "@fontsource/jetbrains-mono/500.css";
